<h2>Take or make a quiz</h2>

<%= link_to "New Quiz", new_user_quiz_path(current_user) %></br>

Your quizzes:
<% @quizzes.each do |quiz| %>
	<li>
		<%= link_to quiz.name, user_quiz_path(current_user, quiz) %> <%= link_to "Take Quiz!", user_quiz_take_path(current_user.id, quiz.id) %>
	</li>
<% end %>