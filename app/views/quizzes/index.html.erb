<h2>Take or make a quiz</h2>

<%= link_to "New Quiz", new_quiz_path %></br>

Your quizzes:
<% @quizzes.each do |quiz| %>
	<li>
		<%= link_to quiz.name, quiz_path(quiz) %> 
		<% if quiz.finished? %>
			Finished! Check your <%= link_to "Score", quiz_results_path(quiz) %>?
		<% else %>
		<%= link_to "Take quiz", question_answer_path(question_id: quiz.questions.first.id) %> 
		<% end %>
	</li>
<% end %>